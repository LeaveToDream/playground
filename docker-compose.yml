services:
  bgg-cards:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - BGG_TOKEN=${BGG_TOKEN}   # set in .env or export in shell before docker compose up
    restart: unless-stopped
